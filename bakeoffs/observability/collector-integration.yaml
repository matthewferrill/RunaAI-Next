receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:9498

exporters:
  file:
    path: artifacts/runs/vertical-integration/traces.json

service:
  telemetry:
    logs:
      level: warn
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]

