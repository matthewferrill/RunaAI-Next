receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:9438

exporters:
  file:
    path: artifacts/runs/stack-bakeoff-otel/traces.json

service:
  telemetry:
    logs:
      level: warn
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
