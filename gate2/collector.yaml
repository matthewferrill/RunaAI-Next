receivers:
  otlp:
    protocols:
      http:
        endpoint: 127.0.0.1:9678

exporters:
  file:
    path: artifacts/runs/gate2-read-only-continuity/traces.json

service:
  telemetry:
    logs:
      level: warn
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
