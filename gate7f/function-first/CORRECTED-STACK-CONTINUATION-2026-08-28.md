# M1-S2 corrected-stack continuation

Selection date:2026-08-28. Source before this record:`793ebc4`.
Roadmap revision:2026-08-28.1; retrieved digest:
`613920536543bcc87dbd1d8bc2e9dca9920f82552c302fc69f92a2fd4a262521`.
Milestone:M1, existing slice:M1-S2. Relevant subsets:C01,C02,C03,C04,C06,C07,C12,C15,C16.
All17 capability families remain open; this is not a new milestone or reduced destination.

## Verified progress and limits

- All360 baseline task attempts completed on the same9556/416102 source/seal,
  with actual application, PostgreSQL/LangGraph, native disposable JS execution,
  Nomic/Qdrant/windowed BGE and required browser checkpoints. The12 formal
  controls and1,266-test frozen Control regression passed separately.
- Coder's independent baseline is73P/10F/37I; Qwen3.6 is79P/6F/35I. Gemma's full
  independent grading remains underway. These are not qualifying role results.
  An inconclusive aggregate does not erase a failed constituent check.
- Baseline defects are retained. Shared prospective fixes preserve exact source
  bytes; preflight the entire plan before effects, including owned undo references;
  use one exact native evidence schema; and clarify continuing user constraints.
  Each has committed criteria and local results. Local model/HTTP fixtures are
  not actual model or live-auxiliary qualification.
- Actual Caddy/TLS wire proof has36 passing cases on its own pinned source,
  including slow-body408,60-second valid primary response,65-second primary and
  15-second reranker deadlines. That evidence cannot be transferred to changed
  Home contracts without a new proof. Prior failed proofs remain retained.
- Native durable intent/return/confirmation and local restart barriers exist.
  Their real host assembly, caller ownership, positive busy-state observation and
  startup/long-idle/recovery still need installed proof. Local task tests alone
  are insufficient. The outer deployment watchdog/adapter is still being tested.

## Continue in this order under the existing authorization

1. Finish independent baseline grading and pin every report, raw reference and
   hardware/cleanup receipt. Do not pool abandoned arms or revise original grades.
2. Independently review the shared fixes. Complete actual native status/owner
   identity, every managed caller's closure/drain, durable private journal and
   bounded deployment process/restart tests. Preserve unowned activity and refuse
   mutation if its outcome is uncertain; do not use unload as a substitute for
   draining accepted work. Root owns integration; separate workers own the Home
   lifecycle, deployment proof and independent grading respectively.
3. Freeze an immutable corrected application/operator/runtime package only after
   those shared implementations are settled. Record exact source/archive/model/
   auxiliary/OS/configuration pins and independent evaluator identity. Run the
   complete isolated Control regression and12 affected formal product controls,
   with actual native executor and live selected auxiliary adapters where needed.
   No guard/schema changes in the middle of a compared model batch.
4. Run the same full three-model, five-function comparison on the corrected shared
   functions. One large model at a time, original role limits, all planned slots,
   all errors/repairs, actual browser observations and verified cleanup. Repeated
   baseline cases are regression/qualification repetitions, not an unseen holdout;
   do not claim otherwise. Preserve>=22/24 acceptable attempts per reported role,
   zero critical model/product failures and100% mandatory controls. Do not raise
   budgets, weaken checks or run only favorable failed-case replacements.
5. Make explicit evidence-backed per-role dispositions. Keep the working
   production route until a qualifying exact successor exists. If a role still
   fails, identify and correct a real shared defect where supported by evidence;
   do not label the stronger failed model qualified. No automatic model-authored
   permissions, fallback or unbounded action scope is allowed.
6. Complete installed Home guard/TLS, startup, long-idle, worker/process loss and
   restart validation. Deploy the exact application/Caddy successor with closed
   admission, durable operation ownership, health verification and preserved
   predecessor. Reconcile uncertain effects before retry or rollback. Rollback
   changes only the candidate route/owned effects and preserves later user data.
7. Fill the exact deployment entry in `M1-CUSTOMER-TRIAL.md`, verify the automated
   customer path, then ask for the real user's sign-in and practical trial. This
   is the human-testing boundary, not another implementation permission request.
   M1 closes only after that trial and reconciliation; M2–M5 remain required.

## Boundaries and reporting

No private conversation, legacy project or protected store is test material.
No new model artifact, broader code environment, live web, connector, public
self-signup or off-LAN activation is implied. Existing owner/passkey/learning
rules and the exact JavaScript capability profile remain unchanged. CLI/browser
tools used by development agents do not become Runa product capabilities.

Retain failures and source pins in the function's result documents and update
`MIGRATION-STATUS.md` when state changes. Commit/push verified work without stopping
at an internal checkpoint. Ask the steward only for genuine human participation.
