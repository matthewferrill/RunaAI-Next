# M1-S1 results: model-neutral role wiring

Date: 2026-08-28. Status: source implemented, independently reviewed and published; not deployed or model-qualified.
Full roadmap: `../../PRODUCT-ROADMAP.md`. Milestone 1 remains open and cannot replace the 17-family roadmap.

## Delivered

1. The authoritative product roadmap, permanent capability inventory, milestone boundaries, evidence
   requirements and mandatory next-slice retrieval workflow are saved in the repository. The first
   roadmap commit is `333912a72ee4252ff87805db4095c69b2abd8921`.
2. Strict, model-neutral role selection supports chat, research, code, review and agent without a
   candidate-name allowlist. Model selection does not grant authority. Local contract commit `daf92c6`
   and independent integration-test commit `c5ea7d1` precede the final wiring/results commit.
3. The real production-composition source now uses independent chat/research/code selections through
   the existing Mastra provider boundary. Disabled roles cannot fall back or save a completed answer.
   Existing grounding, exact-model checking, Code verifier and identity/project boundaries are retained.
4. Versioned v2 configuration, manifest and runtime-status paths report the full role map. A mismatched
   model/configuration pair fails before secrets, stores or sandbox setup. V1 ordinary configurations
   retain their original values/digests and default routing; predecessor files are never overwritten.
5. Qwen3.6's historical failure is documented as a deadline failure with missing runtime observations,
   not an established RCA or exclusion. A bounded separate diagnosis is specified without changing the
   original two-model runner, original judgments or any seal.

## Verification

Environment: Omen, Node 22.22.0. Runs used synthetic input and transient test resources only.
These counts overlap: the full suite includes the focused tests; they are not additive quality scores.

| Check | Result |
|---|---|
| Full `node --test --test-reporter=tap` | **797 passed**, 0 failed/cancelled/skipped/todo; 19.82 seconds |
| Model-role contract | **12/12** |
| Release/provider integration | **15/15** |
| Roadmap guard regressions | **15/15** |
| Original Gate 7F evaluation seal | passed, 5 files |
| Gate 7F v2 evaluation seal | passed, 21 files |
| Gate 7F acceptance seal | passed, 14 files |
| `git diff --check` | passed |
| Independent source/test review | no blocking finding |

The role integration exercises all five existing answer lanes through `Gate2ReadOnlyService`, exact
factory arguments, disabled roles, history preservation, cross-project workspace denial, v1 canonical
digest preservation, v2 manifest round-trip, drift rejection, frozen config values and wrong-model
rejection through the real Mastra adapter with an injected synthetic generator. It does not call a live
model or replace durable-store testing. The historical synthetic v1 manifest digest remains
`14ea5158ab7adf992843dd7d74a604a63c132dc484fbf3faa96e575802c8d434`.

A new cross-project regression initially lacked its authorized destination-project fixture and failed
`project-not-found`. The fixture was completed; the application was not weakened to make it pass.
The final test still proves that a source from another project never reaches a provider.

## Review-driven limits and corrections

- The independent reviewer corrected roadmap tests that otherwise would break after legitimate future
  capability/milestone completion. Unsupported completion claims and missing evidence remain rejected.
- Successor construction intentionally rejects unsafe legacy provider URLs (including all query strings
  and fragments) and whitespace/control-bearing IDs. Historical v1 parsing/digests remain reproducible;
  this narrow safety exception is tested and not described as universal legacy-input compatibility.
- `/models` health is endpoint liveness, not proof every configured role is loaded and ready. V2 status
  reports configuration identity. A future deployment requires exact live requests for every enabled role.
- Review/agent configuration slots do not create those workflows. Separate endpoints, runtime dialects,
  role budgets, actual project tools, persistence/restart, full customer journeys and three-model acceptance
  remain work under M1 or their mapped later roadmap families.

## Publication and preservation

The intended remote is `https://github.com/matthewferrill/RunaAI-Next.git`, branch
`codex/gate7f-agent-foundation`. The first environment check stopped publication before execution; no
workaround or alternate push was attempted. After the steward reaffirmed existing permission, the normal
fast-forward push succeeded on 2026-08-28. The independently queried GitHub branch tip exactly matched
`25494137b755828adaef66b72822a4b1258446d3`, containing all five pending commits. **Publication is complete
through that implementation/results commit.** No PR merge or deployment is claimed, and no additional
permission gate was added to the authorized M1 work.

No live host operations, production route/configuration changes, protected-data access, model downloads
or loads, persistent services, or edits to the legacy RunaAI/RunaLab source repositories occurred in this
increment. Test-only temporary resources were cleaned up. Existing user work and historical evidence
were preserved. No new immutable release artifact was built or customer test requested.

## Next, without losing the destination

Run `node roadmap/read-next-slice.mjs` before selecting further work. Start the next finite customer
function slice with chat/context, while the separate bounded Qwen3.6 readiness diagnosis resolves its
actual artifact/runtime/transport constraints. Then test the real shared functions with all three large
models using prospectively frozen cases. Infrastructure components are not rebaked merely because model
roles are compared. Human involvement is for the useful candidate customer trial, not routine permissions.

This source increment is progress toward the first milestone, not completion of Runa or of M1.
