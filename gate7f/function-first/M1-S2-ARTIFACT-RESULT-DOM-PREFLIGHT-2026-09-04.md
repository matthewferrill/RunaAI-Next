# M1-S2 Artifact result DOM presentation preflight — 2026-09-04

## Result

The bounded Artifact result presentation is implemented in the existing authenticated single-canvas product modules.
The initial bytes passed a focused deterministic preflight; a later broad-suite method attempt stopped, and four
independent implementation reviews then returned STOP at P0=0/P1=3, P0=0/P1=2, P0=0/P1=2 and P0=0/P1=1. The descriptor,
companion, real-module wiring, source-multiplicity, SafeText, dependency-binding and Review mismatch corrections then
received fresh P0=0/P1=0 review. The reviewed method bounded the junction to the seven-test suite, required verified
`finally` cleanup and aggregated suite/cleanup failures before five explicit current-byte syntax checks, roadmap
verification and the diff check. Its one corrected run passed 53/53, removed the authenticated worktree-local junction,
passed all five syntax checks and 15/15 roadmap checks, and completed a clean diff check. Those exact initial bytes are
locally committed at `b6ece37`. The later retained-authority chain is committed through exact Artifact head `bf56905`;
this local primary merge has exact parents `5c6b2e1` and `bf56905` and integrates that full chain. The merge result
remains unpushed, and ordinary authenticated browser/server-authentication acceptance remains required. This record
does not claim a deployed Control release, model acceptance, actual Omen browser proof, production route, customer
acceptance, artifact creation, or completion of C05.

A subsequent actual-browser design review found one retained-control authority gap in those committed bytes: after
the initial authoritative `result.list`, a retained Verify control could issue `result.read` without first proving
that the same owner and descriptor were still current. That review stopped at P0=0/P1=1. The source and deterministic
fixture then received the execution-free correction described below. At that source freeze, the new bytes had not run
a syntax check, test, browser, server, database, model, network, roadmap, commit or push command, and the Artifact DOM
lane was STOP pending a different-agent exact-byte review. The earlier 53/53 result remains valid only for commit
`b6ece37` and does not cover this correction.

Fresh review of that first correction stopped again at P0=0/P1=1: it invalidated retained controls for a missing
owner or browser-detected list/descriptor drift, but treated the production read-race code `result-stale` as a generic
verification failure and re-enabled the controls. No execution followed that verdict. The next source/test/doc freeze
added only the proven `result-stale` invalidation and was completely unexecuted pending another exact-byte review.

Fresh independent review then authorized one corrected deterministic resume. The exact worktree/primary lock digest
and installed `zod@4.4.3` were authenticated, the reviewed worktree-local junction was created, and the exact
seven-file suite passed 58/58 including the new retained-owner and `result-stale` cases. Link-only verified cleanup
left `node_modules` absent. All five explicit current-byte `node --check` commands passed, followed by 15/15 roadmap
verification and `git diff --check`. No actual browser, database, model, provider, Control, production or customer
operation ran. The corrected source/test/evidence bytes are committed at exact Artifact head `bf56905` and integrated
by this local primary merge; the merge result remains unpushed.

## Bound scope

- Roadmap revision/digest: `2026-08-28.1` /
  `7c31dc90109e13f02927cc54fcdd88b41abe093a4cc65a7deaed81f0e99b74f1`.
- Milestone/capabilities: M1-S2; bounded C03, C05 and C15 result-presentation subsets only.
- Source baseline: `971487421821ada884cc992e9a709e7eaeb10601`, including the independently reviewed Artifact
  projection, bounded PostgreSQL owner ports and authenticated `result.list` / `result.read` transport.
- Included: contextual descriptor inventory, format, exact ready byte length, source time, provenance summary,
  readiness explanation, selected ready result read, client-side base64/length/SHA-256/UTF-8 verification, inert
  preview, explicit verified download, and adjacent verified Research/Review metadata.
- Excluded: result creation/editing, Markdown or HTML rendering, links/resource retrieval, syntax highlighting,
  rich or binary formats, upload, filesystem access, model/provider/index calls, browser automation, Control or
  production changes, sharing and publication.

## Implementation

`gate6b/public/artifact-results.mjs` admits only the frozen list, descriptor, provenance, privacy, readiness,
filename and media-type shapes before presentation. It binds exact owner/source identities, kind/format/media/source/
route combinations, frozen conversation/task ordering, same-turn coherence and one-result-per-task-proposal
multiplicity before any content read. It point-reads the already
selected conversation or Code task through the existing workspace request helper. A result identifier or browser DOM
value grants no authority; the server re-authenticates and re-authorizes every request.

Ready content is standard-base64 decoded and independently checked against the descriptor's exact byte length and
SHA-256. A fatal UTF-8 decoder must then accept it before the module assigns it to `textContent`. The module never
uses HTML/Markdown parsing or resource loading. The Download control remains absent until verification succeeds.
Its explicit click creates one Blob from the verified bytes with the descriptor's exact media type, uses the frozen
safe filename on a temporary same-document object URL, clicks one temporary anchor and revokes the URL in `finally`.

Files and artifacts remains an existing single-canvas destination. It resolves the current saved project
conversation, or the exact opened Code task when one is selected. Desktop uses an inventory/preview split inside
that canvas; at narrow widths the verified preview moves below the inventory. Personal/ephemeral or unsaved context
does not call the result surface and says that no result owner is selected. Unknown and reconciliation-required
results stay non-actionable and are not read, retried or relabelled ready. A ready Research or Review report has no
action until its exact same-source ready metadata companion is present. Selection reads and independently verifies
that metadata first, admits its strict positive schema, and only then reads the report. Missing, non-ready, mismatched,
read-failed, digest-failed or invalid-schema metadata leaves the report non-actionable.

The existing HTTP transport remains unchanged: successful and error JSON responses retain `Cache-Control: no-store`
and `X-Content-Type-Options: nosniff`. No `Content-Disposition` response header is claimed.

## Retained-control authority correction

The selected project/experience and initially admitted list are copied into immutable browser-side snapshots. Every retained primary or companion
selection now issues a fresh authenticated `result.list` before its `result.read`. The response must pass the
complete existing list admission and match the frozen owner kind/id,
`ownerRevision`, selected descriptor and, when applicable, exact report companion descriptor. Research and Review
selection revalidates the pair before reading metadata and revalidates it again after metadata verification and
immediately before reading the primary report. The control does not treat cross-tab notification or retained DOM
state as authority.

Missing owners, invalid lists, changed owner revisions, missing descriptors, or any selected/companion descriptor
drift cause the whole retained action set to be disabled and removed. Any old preview and download are cleared and
replaced with bounded missing-or-changed-owner copy; service error text and newly listed result content are not shown.
If authority changes after a valid companion read but before the primary read, the second list gate prevents the
primary read and the already verified companion remains undisclosed. A server-authorized read can still fail after
its immediately preceding list because the two HTTP operations are not one transaction; that failure infers no
success or show bytes. The authoritative missing-owner error and production `result-stale` code both invalidate the
retained controls. `result-stale` is limited to the production projection's exact wrong owner/result/digest/revision
collapse; no other 409 or generic verification error is promoted to an authority conclusion.

The deterministic fixture adds a two-page-equivalent archive transition, owner-revision drift, exact primary and
companion descriptor drift, and a change between companion verification and primary read. It asserts exact service
call order, zero reads when the first revalidation detects drift, no primary read after a later drift, removal and
disablement of retained actions, no Download control, bounded copy, and absence of private service/result canaries.
Existing fresh primary and Research/Review paths now require the additional list-before-each-read calls. At their
source freeze, all of these changes remained unexecuted pending independent review; they are now covered by the one
58/58 corrected deterministic run recorded below.

The latest fixtures also create valid-list/read races where production `result.read` returns `result-stale` for a
primary result and for a report companion. Both cases first create a genuine earlier verified preview and Download
control, then assert that stale detection clears the prior content, removes and disables its Download control and all
Verify controls, renders neither result/error canary, permits no successor control request, and in the companion case
never requests the primary report.

## Deterministic verification and failure accounting

- Four changed/new JavaScript modules/tests passed `node --check`.
- Focused real-module DOM/download plus product-foundation tests passed 12/12 on the pre-review bytes.
- Tests prove descriptor/list/read relationship admission, canonical base64, byte length, digest, fatal UTF-8,
  exact safe filename/media type, inert markup, no script/image/frame/object construction, no object URL before the
  explicit Download click, exact Blob bytes/type/name, URL revocation, companion Research citation visibility,
  non-actionable reconciliation state and digest-failure fail-closed behavior.
- The two focused executions were green (10/10 before the initial companion/non-ready additions, then 12/12); neither
  execution covers the later P1=3 correction.
- One later broad regression command retained 46 passes and two pre-assertion loader failures because the isolated
  UI worktree has no worktree-local dependency tree and the ambient ancestor tree lacks `pg`. It stopped before
  roadmap/diff checks. No successor ran. This is a test-environment selection defect, not an application failure;
  exact RCA and the unexecuted corrected method are
  in `M1-S2-ARTIFACT-DOM-BROAD-SUITE-METHOD-RCA-2026-09-04.md`.
- There was no denied command or blind retry in this lane. The earlier Artifact
  criteria/core/source/fixture review stops remain accounted for in their own immutable records and were not replayed.
- Fresh review of all four corrections returned GO at P0=0/P1=0. The one authorized dependency-bound resume passed
  53/53, authenticated and removed the local junction in `finally`, and left `node_modules` absent. Five current-byte
  syntax checks, 15/15 roadmap checks and the final diff check then passed in the reviewed order.
- Fresh review of the later retained-control and `result-stale` corrections authorized one new resume. Exact lock and
  `zod@4.4.3` identity checks passed; the reviewed junction was created only for the exact seven-file suite, which
  passed 58/58. Verified link-only cleanup left `node_modules` absent. All five current-byte syntax checks, 15/15
  roadmap checks and the final diff check then passed in the reviewed order.

The first independent implementation review stopped at P0=0/P1=3: insufficient browser-side binding of frozen
descriptor relationships/order, insufficient report-to-ready-metadata companion gating, and regex-only evidence for
current-owner/Files wiring. Corrections now add strict source/owner/kind/format/route/order admission, metadata-first
Research/Review verification with fail-closed companion paths, and real-module runtime coverage for current
conversation, opened Code-task precedence and personal/ephemeral/unsaved no-request behavior. Those corrected bytes
remain completely unexecuted pending fresh independent review.

The second independent review stopped the corrected bytes at P0=0/P1=2: same-source multiplicity/coherence remained
underconstrained, and Research `limitation` did not yet enforce frozen `SafeText`. The latest correction rejects
incompatible answer/Research/Review combinations, incoherent same-turn revision/route/evidence/time, multi-kind task
proposal output, unpaired surrogates, NUL/disallowed C0, DEL/C1 and bidi controls. Digest-valid invalid-schema
companions for both Research and Review remain blocked before report read. This correction is also unexecuted.

The third independent review stopped again at P0=0/P1=2: the corrected method still falsely called the selected
closure dependency-free despite its `zod` imports and allowed unreviewed ambient ancestor resolution; the Review
revision-mismatch regression also expected report-specific non-actionable copy rather than whole-list rejection. The
latest method now binds identical worktree/primary lock SHA-256
`cefcc1b9d086fb5eb8088a1be3a1d86fd5b4360bb22aba768c530bbbcf007308`, exact installed `zod@4.4.3`, one
worktree-local `node_modules` junction authenticated as a sole-target `Junction` before import/test, and verified
link-only nonrecursive cleanup. No ambient parent fallback is accepted. The Review regression now requires exact
`result-client-list-invalid`, only `result.list` traffic and no preview/download. This correction is unexecuted.

The fourth independent review returned STOP at P0=0/P1=1 because roadmap and diff checks were still inside the
dependency-junction lifetime and the method omitted explicit syntax checks for the latest changed/new JavaScript
bytes. The method now runs exactly seven tests while the authenticated local junction exists, always authenticates and
removes it in `finally`, proves the path absent, and aggregates suite plus cleanup failures. Only after both are green
does it run explicit `node --check` commands for all five changed/new JavaScript/MJS files, then roadmap verification,
then the diff check. Any failure stops before later checks or commit. This correction is unexecuted.

- Frozen corrected method RCA SHA-256: `210ae5f385079e24d2e4e0bcdcef51c0e08fef7ac7abc9e71be63137b19d0f6a`.

No database, browser, model, provider, index, filesystem-result, Control, production or customer operation ran during
the corrected DOM verification.
The DOM harness invokes the actual shipped Artifact product module; it is deterministic development preflight, not
an actual-system or user acceptance substitute.

## Remaining gate

The four earlier corrected STOPs retain their green 53/53 verification and exact local source commit `b6ece37`. The
later retained-control and `result-stale` corrections now have one separately authorized green 58/58 deterministic
run, verified junction cleanup, five green syntax checks, 15/15 roadmap checks and a clean diff check. They are
committed through exact Artifact head `bf56905` and integrated by this local primary merge with parent `5c6b2e1`; the
merge result remains unpushed. Repository-wide work next freezes this merge's exact commit identity, reconciles Native
from `b064842` with that exact new primary result, obtains fresh combined-byte review, and completes the finite Native
deterministic gate 1 before the frozen ordinary authenticated Omen -> Control browser journey against a built candidate:
current Research/Review and Code results, ready/non-ready state, retained-control owner removal/revision, inert markup,
exact download bytes/type/name/digest, reload and sign-out/sign-in. Actual browser/server-authentication, release,
model, production and customer acceptance remain open.
Any actual-system failure stops that exact gate for retained RCA and corrected-design review before one bounded
resume. Broader C03/C05 formats, artifact production, upload, editing, sharing and publication remain deferred.
