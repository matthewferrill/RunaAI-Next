# M1-S2: real customer functions through the selected stack

Selection: 2026-08-28, source `aa38055cdbf73c72943782707bfe7b8ac9b57478`.
Retrieved roadmap revision `2026-08-28.1`, digest
`f8f8fed9e120700bac3a0613dd6ea75131de0c687961edc324e46b2c5b8e1fbe`.
Milestone M1; capability subsets C01 C02 C03 C04 C06 C07 C12 C15 C16. All other work remains in the roadmap.

The steward expressly requires continued work until M1 is complete or meaningful human testing is
needed. Do not stop after an internal module, commit or push. Existing permission covers this work;
product grants and runtime security remain enforced. Root owns integration and live Home residency;
parallel agents use separate worktrees and bounded files. Continue the published feature branch,
which descends from `runa2/integration` and includes required M1-S1 provider compatibility.

## Baseline and required corrections

M1-S1 has 797 passing local tests and three independently selectable answer roles, not full M1.
Current source has ordinary conversation false denials, browser-supplied rather than authoritative
retained context, no selected-source customer workflow, no deliberate-review lane, and only an
in-memory agent/project adapter. The existing Gate 7E MXC/QuickJS executor provides real bounded JS
execution. Reuse it. PostgreSQL/LangGraph retain durable authority. Current Control release and its
protected data are not test fixtures. Prior sealed evaluations must remain byte-for-byte unchanged.

## Finite work and interfaces

1. Conversation: preserve ordinary writing/explanation, choose grounded versus general context without
   stale-topic inheritance, fetch and authorize retained project/thread context before any provider call,
   reject supplied history as retained authority, and keep anonymous context ephemeral. Reopen/retry and
   concurrent updates must not overwrite or duplicate later turns. Give safe typed dependency failures.
2. Supplied-source research/review: explicitly attach/select non-private text in the candidate workspace,
   apply actor/project/source revision boundaries, use selected retrieval components when searching, and
   show supported citations and honest missing/conflicting evidence. Add deliberate review via the same
   model-neutral provider contract. Retrieved instructions or fake receipts never authorize effects.
3. Disposable projects: implement real application-created contained files, inspect/preview/exact-hash
   edits, selected JavaScript tests through existing isolation, observe/fix failures, and restore only an
   exact owned effect. Reject path/ADS/device/symlink/reparse/hardlink escapes, stale state and concurrent
   modifications. No unrestricted shell, packages, network, host filesystem or external repository.
4. Durable actions: versioned capabilities/profile grants and actor/project/task scope; PostgreSQL task,
   proposal, grant, receipt and uncertain-effect state; LangGraph checkpoints. Execute-time revalidation,
   read-only/ask-every-time/safe-autopilot, expiry/revocation, cancellation, retry and restart reconciliation.
   Unknown effect outcomes require observation, never blind re-execution. Model tool text is a proposal.
5. Customer integration: authenticated ordinary-user candidate routes, Chat/Code separation, source and
   project controls, plan/proposal/progress/result/receipt views and cancel/retry/undo controls. API and
   browser must use the same services. Labels come from trusted results, not model predictions.

## Green criteria fixed before implementation

Retain the eight distinct scenario categories per each of the five M1 functions in
`../../roadmap/CURRENT-SLICE.md`; deterministic control scenarios require 100% success. Add regressions
for ordinary writing/explanation, cross-topic followups, forged browser history, foreign thread/project
access before model invocation, fake execution claims, duplicate requests and stale effect hashes.
Actual filesystem/DB/process outcomes must independently verify the application's receipts.

Run real disposable PostgreSQL restart/concurrent/duplicate/failure checks and actual MXC process tests;
in-memory doubles alone cannot pass these requirements. Validate every affected existing answer lane.
Retain exact source/runtime pins and compare before/after host state. Customer validation uses synthetic
accounts/projects or the steward's explicit test, never private conversations or protected stores.

Before scored model attempts, separately freeze a new three-candidate case bundle, runtime/model hashes,
role context/output/latency and hardware limits. Minimum eight distinct cases/function, three attempts
per participating model. Require >=90% acceptable attempts and zero critical model failures per selected
role; report repaired or contained model errors separately from model success. Readiness/old failures are
unscored regression evidence, not unseen acceptance. Do not omit Qwen3.6 or amend the old two-arm seals.

## Environment, safety and recovery

- Omen: development, isolated local PostgreSQL/services and browser tests, no unrelated repositories.
- Home: read-only baseline first; root-controlled one-model-at-a-time bounded runs on verified artifacts,
  exact owned-instance cleanup, capped source/output/time/process/memory and hardware telemetry. Preserve
  existing production listeners/routing and restore temporary owned settings. Follow the separate
  QWEN36-READINESS-PLAN-2026-08-28.md, including evidence for effective reasoning controls.
- Control: inspect baseline first. Candidate may be built/staged and tested under existing authorization
  with isolated data, exact release config and rollback. Do not modify legacy/protected stores, owner
  credentials or production authority. Production routing changes require the already-defined successful
  candidate/customer/recovery checks; do not equate staging with promotion.
- Abort the owned operation on identity/integrity/isolation loss, unexpected residency, unsafe telemetry,
  unavailable evidence storage or unauthorized effects. Preserve evidence and unowned state. Fix bounded
  defects and continue; ask the steward only for actual human testing/presence.

Rollback means stop/remove only owned disposable services/projects or switch to exact predecessor
application/configuration. Never restore an old production database over later user work. Capability-set
versions pin exact supported actions; adding this executor cannot expand old grants.

## Completion and handoff

Commit/push coherent tested changes and retain failures plus corrections. Do not stop merely to report
an internal gate. M1 remains incomplete until the actual five-function customer journey and three-model
dispositions are ready, then obtain the real human test needed to close it. Broader files, languages,
Git/delivery, live web, connectors, parallel/background product agents and modalities remain M2-M5.
