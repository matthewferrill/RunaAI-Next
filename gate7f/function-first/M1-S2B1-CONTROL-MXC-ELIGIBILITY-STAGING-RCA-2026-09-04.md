# M1-S2B1 Control MXC eligibility staging RCA — 2026-09-04

## Status

Actual-system eligibility is paused. The latest sealed Control envelope read at reviewed commit `ab1b4e7bfcb2f8596083f6a243a644e92372d8f0` stopped before eligibility because the newly published dependency-tree pin did not match the canonical runtime verifier. Local and Control dependency contents match exactly. This is a pin-publication and test-method failure, not dependency drift, an MXC result, or a model result.

## Retained actual result

- Source commit: `606ddc9e06d005ffdff40042b01539c2621ad6e7`
- Control identity: `RUNA-CONTROL\Matthew`
- Sealed Node: `v22.22.0`, SHA-256 `bae898add4643fcf890a83ad8ae56e20dce7e781cab161a53991ceba70c99ffb`
- Result schema: `runaai-native-gate3-mxc-eligibility-operator-error/v1`
- Error: `native-gate3-eligibility-control-preflight-failed`
- Model invoked: false
- Browser invoked: false
- Database attempted: false
- Production changed: false

The failure occurred before `mkdir(root)`, watchdog launch, MXC preflight, or any model admission. It must not be counted as an MXC or model failure.

The first affected-only resume at commit `b43671ce8fb158a74480d2cc48f9e318fdc9c64d` also stopped before product-directory creation with `native-gate3-eligibility-control-parent-preflight-failed`. Its result again records model/browser/database/production effects as false.

## Root cause

The proposed operation root used the shared `C:\AI\RunaAI-Next-Candidate\staging` directory. Its identity, canonical path, owner, and non-reparse topology were valid, but its ACL contains Authenticated Users rules with real Modify/Delete-child authority. A private child created below that parent could therefore be replaced through parent authority during an operation. The fail-closed rejection was correct.

The diagnostic also exposed a classifier defect: the original mutation mask combined the Windows `Write` and `Modify` composite values. Those composites contain the `Synchronize` bit, so three read-only application-package/Users rules were incorrectly counted as writable. Two Authenticated Users rules were genuine mutation authority; the unsafe result was real even though the count of five was inflated.

The affected-only resume exposed a second operator defect. The ACL predicates all passed, but the stripped PowerShell child auto-loaded `Get-Acl` and emitted 616 bytes of benign `Preparing modules for first use` progress as CLIXML on stderr. It exited zero and returned exactly `ok` on stdout. The strict wrapper correctly rejects unexplained stderr, but use of a module-loading cmdlet made benign host initialization indistinguishable from an error channel. This was an operator invocation/publication failure, not a staging, MXC, or model failure.

The next affected-only resume at commit `3772a76de84af38f3c95126e5a7503398caeb12d` stopped before product-directory creation with `native-gate3-eligibility-source-snapshot-dirty`. The original source gate collapsed commit format, tree format, tracked membership, and whole-worktree status into one error and retained no predicate-level result. Immediately afterward, the exported source-authority function and every constituent predicate passed against the same commit and tree. The exact transient predicate therefore cannot be recovered from the published error. That inability to classify is itself an operator-publication defect; it is not evidence against MXC or a model.

The affected resume at commit `7d2c57c36998e29c663c94b5f94f3d05c13a4f0d` first passed the exact read-only source/dependency envelope, then again stopped before product-directory creation with `native-gate3-eligibility-control-parent-preflight-failed`. An exact reproduction under the stripped child environment showed status 0, stdout exactly `ok`, and one 392-byte benign module-analysis progress record on stderr. Replacing `Get-Acl` was insufficient because other PowerShell cmdlets can trigger the same first-use progress. Re-running that exact read-only child script with `ProgressPreference=SilentlyContinue` produced status 0, stdout `ok`, and zero stderr bytes. The incomplete prior correction and its differently configured direct check did not cover the true child environment.

The affected resume at commit `3c2e9af188dd4a751159eb057a92cdbb99bff13b` proved that the centralized progress suppression worked, then stopped before watchdog preparation or MXC with `native-gate3-eligibility-acl-invalid`. The retained public result records `scratchRemoved: true`, no cleanup failures, and no model, browser, database, or production effect. Read-only ACL inspection found that the product hierarchy and operation root were correctly owner-private, but the newly created `journal` directory had owner `S-1-5-32-544` (Administrators), inherited rules, and an unprotected ACL. The operator had explicitly privatized only the operation root and assumed child directories would inherit the complete private boundary. Windows inherits access rules, but a new directory's owner is assigned from the creating token and ACL protection is not inherited as an explicit child property. The verifier correctly rejected that mismatch. This is a provisioning-design defect, not an MXC or model result.

The affected resume at commit `d0f8977e294113ebf19b6827c4a745e7c2b843ff` passed source admission, the parent gate, explicit ACL provisioning for `root`, `journal`, `scratch`, `localappdata`, `temp`, and `transient`, and request sealing. It then stopped before watchdog process creation with primary failure `native-gate3-eligibility-acl-invalid`. Because a request had already been prepared but no terminal process record existed, conservative cleanup retained scratch and the top-level projection correctly reported `native-gate3-eligibility-and-cleanup-failed`; the retained public result separately records the primary ACL failure and `native-gate3-eligibility-scratch-retained`. Read-only inspection identified the two remaining directories created inside the shared watchdog launcher, `journal\host-localappdata` and `journal\host-temp`. Both had the same Administrators owner, inherited rules, and unprotected ACL. All six operator-created directories were correctly protected and owner-private. This extends the same provisioning defect into the shared v2 watchdog layer; it is still before MXC or any model, browser, database, or production effect.

The affected resume at commit `22dd1b28288f3180fd626c2c066d8a74dc7cc8e8` passed every ACL and source boundary, launched the independently bounded watchdog, and completed the actual MXC preflight successfully: the retained child result is `passed: true`, status `executed`, isolation tier `appcontainer-dacl`, exit code 0, and 20 expected output bytes. The supervised child also exited 0, all processes and descendants were absent, output was complete and within bounds, and scratch cleanup succeeded. Its 582-byte stdout is exactly the byte length of the expected acknowledgement JSON for the retained operation facts. Admission nevertheless remained unacknowledged because the child also emitted 97 stderr bytes; the strict wrapper requires zero stderr and therefore retained terminal outcome `unknown`, returned host exit 2, and the operator stopped with `native-gate3-eligibility-supervisor-completion-unknown`. This is an evidence-channel failure after a successful MXC result, not an MXC or model failure.

The exact content of the first 97-byte event cannot be recovered: the shared native helper intentionally drained stderr without retaining raw text. Version 3 corrected that publication gap by retaining the bounded byte count and SHA-256 `b6ca69050185767a482c1e2f5743372219b34bd94cf97630cb673b8353645e6a`, plus the independent acknowledgement-candidate result, without weakening admission.

The affected-only resume at commit `d20398cad8554501aafc92bf4aa0f20c5b884aee` proved the version-3 evidence channel works and disproved the earlier Node-warning hypothesis. MXC again passed with status `executed`, isolation tier `appcontainer-dacl`, child exit 0 and 20 output bytes. The acknowledgement candidate was valid, the supervised child exited 0, all processes were absent, output was complete, and scratch was removed. Admission correctly remained false because one 97-byte stderr event with the same SHA-256 remained after exact `--no-warnings` binding.

A separate bounded actual-Control diagnostic used the same replacement environment, sealed Node and real MXC preflight. It intercepted one `process.stderr.write` call with exactly the retained 97-byte length and SHA-256, classified the call frame as `@microsoft/mxc-sdk:dist/diagnostic.js`, recorded no `console.warn`/`console.error` call, and observed zero operating-system stderr after interception. The installed `@microsoft/mxc-sdk@0.8.0` source eagerly evaluates `getDiagnosticPipeName()` at module load. That function executes shell-form `whoami /user /fo csv /nh` before `diagLog()` checks whether `MXC_DIAG_CONSOLE` is enabled. Thus a disabled optional diagnostic feature still starts an ambient shell probe whose stderr reaches the importing process. The diagnostic directory was owner-private and was removed after the safe aggregate was captured. No model, browser, database or production action occurred.

The first focused Control verification of the applied dependency patch stopped before eligibility because the regression test resolved its installed SDK path and child working directory with `path.resolve(...)` from the SSH session's ambient `C:\Users\Matthew` working directory. All three cases consequently targeted a nonexistent home-directory `node_modules`; no test reached the patched dependency and no model, browser, database, production or eligibility action occurred. The patcher itself had already completed with the exact patched SHA-256. This is a test-entry portability defect. The correction derives one repository root from `import.meta.dirname` and uses it for both the installed target and the child import working directory, eliminating ambient-cwd dependence for local, SSH and future install verification.

After that correction passed all three focused cases on Control, the sealed dependency-envelope read stopped before eligibility with `native-gate3-eligibility-dependency-drift`. Read-only comparison proved that all four dependency roots on local and Control had identical file counts, directory counts, byte counts, and per-root digests. The runtime verifier's one ordered record stream deterministically produced `fc56863df60137cafc0f6770cf62de031d2abed644743347aca3247c3140e7ea`; the published pin was `88260d397be0fff04201336fa95cd02190fe05f07edd4aa853f3ffe6ee2b2daa`. This is a pin-publication defect caused by calculating the replacement pin outside the canonical runtime implementation, not dependency drift, an MXC result, or a model result. No eligibility, model, browser, database, network, or production action occurred.

## Systemic correction

1. Use the current Control identity's existing `C:\Users\Matthew\AppData\Local` boundary as the trusted parent. Read-only inspection proved it has no reparse ancestors, an allowed owner, and zero untrusted applicable or inheritable mutation rules.
2. Authenticate the exact reviewed source commit and six active-method source hashes before creating any product directory.
3. Provision only the product-owned `RunaAI\Gate7F\staging` hierarchy below that trusted parent. Apply and verify the existing owner-plus-SYSTEM private ACL at each new level. Do not modify ACLs on `C:\`, `C:\AI`, the user profile, or `AppData\Local`.
4. Detect mutation authority using only primitive write/create/delete/ACL-ownership bits. Do not use Windows composite masks that overlap read-only `Synchronize`.
5. Continue to reject every reparse ancestor and every existing product directory that is not already the exact owner-private boundary.
6. Use the .NET `System.IO.Directory` ACL APIs for get/set operations in all three boundary helpers. They avoid the module auto-load progress channel while retaining strict rejection of any nonempty stderr; do not weaken the stderr gate.
7. Replace the broad, non-evidentiary whole-worktree status predicate with exact admission of every executable source input: the reviewed external commit and six active-method hashes (including the operator's imported watchdog module), hard-coded executor/contracts/lock hashes, tracked membership for all nine source inputs, the exact dependency-tree digest, and the already-sealed runtime/runner hashes. Emit a distinct error for commit format, tree format, and membership instead of collapsing them.
8. Include `zod` in the source dependency-tree digest because the admitted contracts module imports it. The fixed release's QuickJS runtime remains governed by its existing sealed release-artifact proof; this eligibility gate does not reconstruct or replace that release proof.
9. Set `ProgressPreference=SilentlyContinue` centrally inside the exact encoded script passed to every PowerShell helper. Keep the status, signal, spawn-error, exact-stdout, and zero-stderr checks unchanged. This removes only PowerShell's nondiagnostic progress stream and closes the whole helper class instead of chasing individual cmdlets.
10. Apply and verify the owner-private ACL to every directory created for an operation (`root`, `journal`, `scratch`, `localappdata`, `temp`, and `transient`) instead of assuming owner and ACL-protection semantics propagate from the parent. The shared creation helper performs create, ACL replacement, and verification as one fail-closed sequence.
11. Make the verifier require a protected ACL and reject inherited rules in addition to requiring exactly the current owner and SYSTEM with Full Control. This ensures every accepted directory has an explicit, self-contained private boundary.
12. Require every protected v2/v3 watchdog caller to provide a trusted `createOwnerPrivate` authority method. Use it for `host-localappdata` and `host-temp`, then independently re-run the existing directory-boundary verification before process creation. The existing closed-companion adapter emits v1 requests and remains unchanged; the new authority is required only when a caller actually emits the protected contract.
13. Preserve conservative scratch retention whenever request preparation succeeded but no authenticated terminal record proves process absence. The aggregate top-level failure does not replace the durable public result, which records primary and cleanup codes separately. Do not delete the retained `d0f8977` scratch tree as part of a retry.
14. Invoke the pinned outer Node bootstrap with the exact v3-only argument vector `--no-warnings`, followed by the single reviewed bootstrap path. Bind the flag and order in the JS request contract, PowerShell wrapper, argv digest, and bootstrap `process.execArgv` check. This suppresses Node's warning machinery only; direct stderr writes, thrown errors, exit status, and the zero-stderr admission requirement remain fail-closed. V1 is unchanged and retained v2 is read-only.
15. Emit request and terminal version 3 for the warning-bound path while preserving read-only validation of retained version 2 evidence. New preparation always emits v3 and launch rejects legacy v2 as read-only, so the compatibility path cannot authorize a replay.
16. Replace the lossy v2 terminal projection with version 3. When bounded drains complete without limit or fault, compute SHA-256 for full stdout and stderr, clear the retained byte buffers, and publish only hashes, byte counts, and stderr classification `none` or `unclassified`; otherwise publish null hashes and `unavailable`. Retain stderr bytes in memory only for v2/v3 admission processing, zero each pump buffer, and unconditionally clear and dispose both sink buffers after their readers end. Never retain raw stderr.
17. Validate the acknowledgement candidate independently of stderr and publish only `AcknowledgementCandidateValid` plus its stdout digest. Final `AdmissionAcknowledged` still requires a valid bound candidate and zero stderr classified `none`. Thus the evidence can distinguish a valid acknowledgement plus nondiagnostic-channel collision from a malformed or adversarial acknowledgement without allowing either to green.
18. Keep v1 request, stderr-retention, record shape, and behavior unchanged.
19. Correct the dependency at its source boundary rather than filtering its stderr. In the installed MXC diagnostic module, compute the per-user pipe name lazily inside `ensurePipe()` only after `diagLog()` has proved diagnostics enabled. Replace shell-form `execSync` with `execFileSync` against exact `C:\Windows\System32\whoami.exe`, fixed separate arguments, and `stdio: ['ignore', 'pipe', 'pipe']`.
20. Apply the correction through a tracked, idempotent `postinstall` patcher for exact package `@microsoft/mxc-sdk@0.8.0`. The patcher accepts only the exact vulnerable or patched SHA-256, requires the repository-local `node_modules` hierarchy to be canonical ordinary directories rather than links/junctions, requires the target and package manifest to be ordinary one-link files, and fails before mutation on any unknown/version/path/hash state.
21. For the vulnerable-to-patched transition, revalidate the held target immediately before writing, write through the existing one-link file handle, fsync, then reopen and verify the exact patched SHA-256. A previously patched install is a verified no-op. This prevents a shared dependency junction, package upgrade, partial prior edit or ambient vendor drift from being silently modified.
22. Regenerate the lock with pinned Node `v22.22.0` and npm `10.9.4` in package-lock-only, offline, scripts-disabled mode. The only lifecycle delta is root `hasInstallScript: true`. Pin the resulting Control-CRLF lock SHA-256 `c5d2f92ac4066fb5ee6f7def8174802a1659b5e8b3d68eba152fb996ac949821`; the lock records upstream integrity and the tracked install hook.
23. The first replacement-pin publication attempted to recompute the complete four-package dependency-tree digest after the exact patch and published `88260d397be0fff04201336fa95cd02190fe05f07edd4aa853f3ffe6ee2b2daa`. This step is superseded because it did not use the runtime verifier's canonical ordered record stream.
24. Replace the incorrectly published dependency pin with the canonical runtime stream result `fc56863df60137cafc0f6770cf62de031d2abed644743347aca3247c3140e7ea`. Keep the fixed package roots and hashing algorithm in the bootstrap, export that exact implementation, and make runtime admission compare its result with the published constant.
25. Generate and verify future pins through a tracked command that imports the bootstrap's shared implementation. Its focused test launches from a non-repository working directory, compares the generated digest with the runtime pin, asserts the fixed root order, and proves a dependency-tree mutation changes the digest. It never enters eligibility or invokes a model, browser, database, network, or production path.

This is the reusable correction for every native operator: validate the parent that can replace the child, use primitive rights for ACL classification, authenticate code before mutation, and make the product-owned working hierarchy private before placing evidence or capabilities in it.

## Resume gate

No retry is permitted until the shared runtime/generator implementation, corrected dependency pin, cwd-independent generator test, mutation test, and updated bootstrap authority pass syntax/diff checks, focused local and Control verification, and independent review with P0=0/P1=0. The exact dependency patch and canonical manifest must match in the real local and Control trees. After publication, only the stopped affected Control eligibility invocation may be resumed. Any new actual failure stops the campaign again for retained evidence and RCA.
