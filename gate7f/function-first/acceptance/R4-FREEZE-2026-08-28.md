# M1 R4 prospective freeze

This is a fresh qualification run, not a replacement grade for R1, R2 or R3.
Milestone M1-S2 remains incomplete; the other seventeen capability families remain tracked.

## Exact application and common runtime

- Application commit: `ec0a63d974e53ac7e19a2a6bae1c6caa40fc1a8a`.
- Source archive: `724a884d3d2b6429b12c31582f3ac73456943c591f7c50ac8286c9bde1aa505c`.
- Common runtime seal: `evidence/campaign-20260828-r4/runtime-seal.json`.
- Seal SHA-256: `ed651b437e3313addce16e2b185a5ef1b212f54e7d325418eec1a82519fd21b5`.

Only the application commit and its source archive change in the common seal from R3.
The three model artifacts, five roles, forty unique cases, three repetitions, 360-attempt
denominator, twelve model-free controls, budgets and thresholds are unchanged. Order is
Coder, Qwen3.6, Gemma. Each candidate must finish its 120 attempts and receive independent
semantic review; no winner or qualified role is presumed.

The application includes the prospective approval-planning clarification, read-only snapshot
explanation protocol and conditional-evidence correction. Their criteria, tests and limitations
are retained in the corresponding results documents. The conditional reader does not treat
missing precursor work as proof of either successful protection or an unsafe effect.
It still fails observed unauthorized dispatch/publication and exact-provenance contradictions.
Earlier raw flags, grades and stopped-run denominators are not rewritten.

## Preflight evidence and remaining entry checks

Omen's complete real-PostgreSQL invocation at the frozen commit ran 1,259 tests: 1,254 passed,
five failed, zero skipped. All five failures were actual MXC startup with the default whole
checkout/profile-temp envelope, returning unavailable rather than invented execution.
The independent worker-crash scenario passed. The database was stopped and only its owned
synthetic data removed. This matches the previously documented envelope limitation in
`../tasks/RESULTS-2026-08-28.md`; it does not establish one particular Omen setting as its cause.

The initial failed invocation is retained in ignored local
`artifacts/runs/m1-r4-integrated-regression/`:

- `result.json`: `c437aefd1961a3788fe9c0794c5c021ae6b07421f91296a81dd7ca5f12535b19`.
- `tests.txt`: `8ae160177e4c8e8c33ea1dc33af50afa906e05285b0b88232cf4ec272d55f637`.

Before any R4 scored model request: complete the full suite in the exact owned compact
Control runtime envelope, verify all four fresh staged source/dependency sets, and obtain
twelve qualified fresh controls. Actual browser acknowledgements must carry the observed
check kind and ID and reference actual DOM evidence. A completed driver alone is not a pass.
Any failed prerequisite remains a failure; no environment test is silently skipped.

New offline R4 Home lease packets retain the same hardware envelope and one-large-model
rule. Packet creation is not model loading. Root owns campaign dispatch; the Home operator
alone owns load/unload/telemetry. Guard-development work remains separate from this frozen
application and may not change the running engine during qualification.

The release, protected stores and production routing are unchanged. Operational Home guard
proof, rollback-protected deployment and the bounded human customer trial remain required
after functional qualification. Existing steward permission covers continued work; internal
commits and successful test subsets are not stopping points.
