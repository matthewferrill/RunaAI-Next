# M1 R4b: corrected package, unchanged functional qualification

Application commit: `9556ed01f9dbabe8c93eea309e482aad60bf809f`.
Archive SHA-256: `e10adce53387bcf31b639738e2d7ae26c2b5dd17e2914f1870ba0ef1949b31dc`.
Common seal: `evidence/campaign-20260828-r4b/runtime-seal.json`.
Seal SHA-256: `416102ff7129e5adb00de51b2f0fc3e5ca542c18a82941a32fdc4075b6a1c89f`.

R4 stopped at preflight, before any model request. Its original archive, seal and failed
full-suite records remain intact. R4b changes only application/source-archive identity in
that common seal. Models, requests, roles, cases, thresholds, 360 attempts, twelve controls,
independent semantic review, hardware policy and customer-trial requirement are unchanged.
The already-prepared but never-dispatched R4 Home lease packets remain the finite hardware
operators; their exact seals and readiness receipts must still be independently checked.

The application difference is packaging/test portability, not a model or action-policy
change. Four exact-path attributes protect historical frozen bytes outside the existing
sealed LF scope. The lockfile's raw CRLF bytes now match the historical/deployed pin;
parsed dependency JSON is identical. No historical seal or loaded-source check changed.
The Qdrant test now forwards its host's explicit pinned binary through the builder's
existing argument; the artifact verification is unchanged.

Root's focused run passed 31/31 with zero skips: six frozen-byte tests, seven Qdrant
tests and eighteen historical publication tests (seventeen of those had failed in R4).
Independent audit checked all fourteen historical entries in Git, working files and both
autocrlf archive modes against their original lengths/hashes. New negative tests still
reject line-ending rewrites and invalid binaries. The deliberate CRLF lock preservation
requires command-scoped `core.whitespace=cr-at-eol` for Git's whitespace check; no verifier
normalizes bytes or accepts an alternate hash.

Four fresh Control stages are used; none of the failed stages is repaired in place.
Before scored requests: verify complete extracted source/dependency identity, run the
whole suite with actual disposable PostgreSQL and the reviewed compact MXC envelope plus
the explicit staged Qdrant binary, then qualify all twelve fresh controls. The six actual
native project cases passed in the prior R4 compact run, but that does not replace this
complete fresh verification.

Production, identity, protected stores and routing remain unchanged. Continue through
three-model qualification, operational guard proof and rollback-protected deployment
until genuine customer testing is ready. This freeze is not M1 completion or another
owner-permission gate. All seventeen roadmap families remain open.
