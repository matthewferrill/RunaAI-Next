# Retained independent Coder replacement review

The four report files are exact byte copies of the reviewed artifact outputs. The complete raw observations, ledger records, semantic sidecars, original full grades, decisions, frozen-reader audit and control review remain unchanged at:

`D:\AI\CodexHome\visualizations\2026\08\20\01a02109-d801-7c71-a69e-511f1ddd5278\independent-semantic-9556ed0-r4b-replacement-01`

Resolve `rawFile`, `recordFile` and `sidecarFile` in the dual-grade ledger relative to that artifact directory, not this compact repository directory. The original full grade is at `rawFile#/grade`; the complete reviewed grade and quoted rationales are at `sidecarFile#/reviewedGrade` and `#/evidence`. Each is hash-bound. The retained final audit SHA256 is `94f4aadb1ef8d0ca5f1b1ca48bd59360e0e27c8b73e46298a6fe0fc7ab9cbf0d`.

This snapshot covers the completed120-attempt Coder replacement only. Its360-slot manifest does not classify the later Qwen or Gemma arms, assert they never executed, or supersede their later evidence. Original stopped/aborted campaigns remain separate. No qualification or production-readiness claim is made.

The first integration exposed a checkout-only CRLF conversion: the recorded artifact and Git-blob hashes were correct, but an automatic-text checkout changed report bytes. The directory attributes now disable text conversion for these exact retained files. The original four artifact hashes remain authoritative; no normalized alternate hash, raw observation, semantic decision or grade was substituted. BYTE-PRESERVATION-VERIFICATION.json retains the exact working-file, Git-blob and archive comparison.
