# Retained native observations are byte-pinned; never normalize their line endings.
evidence/** -text
# The transport proof pins the exact operator script, not a normalized checkout representation.
tls-enrollment-operator.mjs -text
prove-tls-transport.mjs -text
# The real Caddy wire proof binds the exact body-timeout implementation bytes.
proxy.mjs -text
# One-off owner probe packages retain these exact reviewed source bytes.
Run-HomeOwnerStatus.ps1 -text
owner-status-operator.mjs -text
probe-native-remote-status.mjs -text
