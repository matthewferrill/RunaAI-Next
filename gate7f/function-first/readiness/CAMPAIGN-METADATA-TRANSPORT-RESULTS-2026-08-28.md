# Metadata publisher transport integration

The mirror copies the exact tested helper once through bounded SSH stdin into
the owned stage's evidence directory, names it by SHA256, refuses existing
different bytes/reparse targets, and checks that pin before every publication.
The helper source is not embedded in repeated encoded-command arguments, which
would risk Windows command-line limits. This creates no service and changes no
frozen application file, observer deadline, schema, score or model request.

The fresh Coder R5 mirror completed its actual startup through this path before
scored attempts began at source9556/common seal416102. The continuing campaign
consumes fresh hardware observations without reader retries. Writer sharing
retries are included in the mirror's final receipt. The helper's actual Control
seven-test proof and its original criteria bytes remain separately pinned in
`CAMPAIGN-METADATA-PUBLICATION-CONTROL-REVIEW-2026-08-28.md`.

The integrated Home runtime regression at root additionally passed104/104,
zero skips, in isolated fixtures. Raw logSHA256:
`6a282af44fad18e7b96243078ce47dd62b8a73d27c5979295e3cebe03d242631`.
This is not installed Home admission/drain, TLS, long-idle, or recovery proof.
