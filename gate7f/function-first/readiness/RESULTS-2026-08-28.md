# M1 prospective transport and hardware readiness — 2026-08-28

Status: bounded readiness diagnostics completed, including retained failures and limitations below.

This is a readiness diagnostic, not a new bake-off, product-function score, model promotion or M1
completion. It leaves the approved stack selection and all three primary candidates intact. It neither
proves nor rewrites the earlier Gate1 timeout cause: that historical call lacked the required wire,
reasoning and runtime evidence. All model inputs/outputs retained here are synthetic.

## Present Qwen failure reproduced and controlled

The exact Qwen3.6 MTP artifact, current pinned Vulkan2.28.2 backend and current GGUF template were
verified before loading. The original-style clean evidence envelope was reconstructed prospectively.
The model was loaded at32,768 context, with instance-local MTP enabled and draft2/min0/probability0.75.
The differently sized Qwen3.6 base artifact was not substituted.

At the sealed r3 controlled160W setting:

| Transport/control | Repetitions | Observed outcome | Elapsed seconds |
| --- | ---: | --- | --- |
| `/api/v0/chat/completions`, text `/no_think` only | 3 |512 reasoning tokens, no answer, length limit each time |22.22–22.46 |
| `/v1/chat/completions`, text `/no_think` only | 3 |512 reasoning tokens, no answer, length limit each time |22.79–23.17 |
| Same `/v1` request plus `reasoning_effort:"none"` | 3 |64 output tokens, zero reasoning tokens, answer, normal stop |3.03–3.32 |
| Native `/api/v1/chat`, `reasoning:"off"` | 3 |64 output tokens, zero reasoning tokens, answer |3.02–3.05 |
| Native explicit reasoning-on control,128-token limit | 1 |128 reasoning tokens, no answer |6.66 |

The matched `/v1` pair changes only the API reasoning control. This establishes that the text suffix
did not turn reasoning off in the tested configuration, while the explicit API control changed observed
behavior. HTTP200 alone is not the proof. Raw requests, output channels, token usage and the installed
request-to-template mapping provide the evidence. Missing effective-setting response echoes remain
`null`; they were not invented.

The off arms retain the same text suffix to isolate the variable. The application's proposed no-suffix
request shape must still pass its real M1 smoke/function cases. The native endpoint comparison also
changes its request adapter and is not presented as a single-factor endpoint experiment.

## Context and deadline limits

Qwen returned a28-token answer to4,770 prompt tokens in17.740s. The first14,270-token request timed out
at30.004s. After two low-utilization samples, a separately labelled120s diagnostic returned in9.458s.
Repeated prompt/cache state can affect that retry; it does not erase the failed30s request or qualify
14k cold prompts for30s. The larger1,400-row rung was not attempted after the first timeout. GPU idleness is not a
server cancellation acknowledgement, so endpoint cancellation/exclusive scheduling remains a separate
application-runtime responsibility.

One small successful response at a particular context is not a general latency SLO for every function.
Cold model loading, prompt processing and token generation must remain separate in product reporting.
One-owned-model residency was checked; exclusive traffic to the shared LM Studio endpoint was not
independently proven. The captures explicitly retain `exclusiveTrafficVerified:false`.

Gemma's r4 compatible explicit-none requests returned answers with zero reasoning, normal stop and77
output tokens in1.321–1.492s (3/3). Its measured context ladder was4,525 prompt tokens in3.747s,
13,525 in9.191s, and25,625 in19.068s; all returned within30s. These are observed tokenizer counts,
not the rough4k/12k/22k design shorthand. It unloaded its exact instance and reported cleanup verified.

Coder's r4 compatible requests omitted `reasoning_effort` because this model does not advertise a
reasoning toggle. The generic case ID `v1-api-off` does not mean the field was sent. Its three replies
returned75 output tokens, zero reasoning, normal stop in0.959–1.271s. The context ladder measured4,514
prompt tokens in3.428s,13,514 in10.719s, and25,614 in24.651s. All were inside30s. Cold load durations
were17.743s Gemma and27.546s Coder, separately retained from warm inference.

## What this allows next

Use an explicit, versioned application request control for reasoning-capable candidates, bound to the
exact model/runtime/release configuration; do not infer the setting from a prompt suffix. Keep the
specialized Coder candidate separate and omit controls its runtime does not advertise. The next work
is the actual M1 chat/continuity, approved-source research, disposable-project coding, durable action
workflow and review cases through the real application path for each candidate. These readiness calls
do not replace any of those function tests or the broader17-family product roadmap.

Any Nomic-plus-primary research residency needs its own prospective resource envelope. No embedding
model was added to these sealed diagnostic arms; the existing8412 BGE service was left alone.

## Thermal boundary and retained failed run

The first r2 run preserved the existing260W limits. During its third Qwen text-only request, GPU1
reached85C and the runner aborted. GPU0 was79C. Two completed attempts had already spent all512 tokens
on reasoning. The owned model instance was unloaded and cleanup verified; the failing telemetry and
partial capture remain in `evidence/20260828-r2-thermal-stop/`.

A new package was sealed before any r3 call. It applies the previously reviewed160W envelope equally
to all candidates, waits for<=45C before each arm, keeps the85C abort, and restores original260W in
`finally` only after verified model cleanup. It does not change clocks, fans, firmware, global model
defaults, services, production routes or Control files. The r3 latency figures are therefore160W
diagnostic figures, not claims about default260W production performance.

## Interrupted transport and independent supervisor

r3 Qwen completed; Gemma's child stopped immediately after a successful unscored warmup. Its runner
never wrote a result or completed its own cleanup. The wrapper did attempt its `finally`: at15:50:23Z
it retained `readiness-power-wrapper-failed` and `powerRestored:false`, because the Gemma instance was
still present. This is distinct from claiming the wrapper's `finally` never ran. The underlying child/
native-stream failure cannot be established from the incomplete old stream capture.

Exact-scope recovery verified the package seal and captured Gemma load identity, unloaded only that
owned instance, verified zero residency, and restored both original260W at15:57:42Z. The old failed
power receipt and the later successful recovery receipt are both retained. No result was fabricated
for the incomplete Gemma arm.

r4 freezes unchanged request/model/hardware criteria but moves the remaining Gemma/Coder arms to a
one-off Home Task Scheduler supervisor independent of SSH, with bounded local stdout/stderr,5s durable
watchdog samples, targeted PID/start-time cancellation and captured-instance recovery. A no-model
scheduler probe passed before its dispatch. That lifecycle change does not retroactively complete r3.

r4 completed both arms and restored260W at16:14:36Z. Gemma peaked67C, Coder74C; maximum recorded sample
gaps were5.459s and5.037s. The task returned0 and its completed registration was removed. Final live
inventory confirmed zero model instances. Child `ExitCode` fields are null due the PS5 process-handle
capture behavior; do not reinterpret those as child exit0. Complete runner results, raw captures,
cleanup, supervisor task result and power restoration are separate retained evidence. The subsequent
lease supervisor retains the live native process handle; a no-model child exiting17 proved exact exit
capture locally. The completed r4 runs are not repeated to replace this evidentiary limitation.

## Reproduction and evidence integrity

- r2 seal: `67c3041790b3b0a806625ad6e4e0b49d73f0f36293b4a31176cb88c03946a1e3`.
- r3 seal: `4a17bf88c5d77af50b2c46b9063d1ded7f42dd114f9bb78f7d0754cd048bbe10`.
- r4 seal: `dda05bd43db3d445d093224d7345bf86522e2ef1824b2018f932e4acd656ae88`, created16:07:36Z
  from commit `a2518ed1c980d0450fa12b9558a40038a519cf03` before r4 model calls.
- `verify-export.mjs` recomputes byte/request hashes, raw-response and recorded-observation bindings,
  actual token caps, reasoning claims and cleanup evidence; it never grades answer semantics.
- The readiness source/negative-verifier suite passes18/18 locally. This is not an18-case live-model
  product score.
- `evidence/** -text` preserves exact retained evidence bytes across Git checkouts.

Relevant primary API contracts: [native chat](https://lmstudio.ai/docs/developer/rest/chat),
[load and instance configuration](https://lmstudio.ai/docs/developer/rest/load), and
[compatible chat](https://lmstudio.ai/docs/developer/openai-compat/chat-completions). The last page does
not document `reasoning_effort`; its mapping was verified in the exact installed index.js and then
tested on the actual runtime. No inference from a generic documentation claim substitutes for the
recorded requests and responses.
