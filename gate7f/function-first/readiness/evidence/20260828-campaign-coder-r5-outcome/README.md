# CoderR5 hardware lease completed; model quality is separate

Root reported its unchanged9556/416102 application campaign completed120/120 with no skipped attempts
or stop code, but productQualificationPassed:false and independent semantic review pending. These
application facts are not inferred or graded by this hardware packet.

The exact Coder/Nomic lease completed normally at2026-08-28T21:50:17.542Z; both owned instances were
unloaded, worker/supervisor returned0, and the final independent21:50:57.1827586Z observation found no
resident models or owned campaign tasks and both exact GPUs restored to260W. All587 telemetry samples
are retained; peak77C, maximum sample gap5027ms, minimum GPU headroom7080MiB. No operator inference,
production route, native configuration or TLS enrollment occurred.

The initial companion dispatcher failed before PowerShell with "The command line is too long".
A read-only check established that the exact completion marker and writer package were absent while
the lease task was running. Criteria0a1173d and corrected dispatcherbc05283 reused the independently
tested short hash-bound stdin bootstrap, preserving the identical writer and marker. Fourteen local
tests and an actual read-only Home UTF8 echo passed before the new explicit attempt. There was one
actual publication at21:50:08.4583325Z, followed by normal owned cleanup, not an automatic replay.
`completion-publication.json` retains the writer bytes/hash, transport script hash and raw receipt.
The earlier failed transport remains documented; it is not relabeled as a successful call.

`summary.json` hashes every retained source, telemetry, receipt and final-observation byte. This is a
successful hardware lifecycle result, not qualification of Coder for any application role.
