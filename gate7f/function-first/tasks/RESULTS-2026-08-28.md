# M1 task/orchestration component evidence, 2026-08-28

Acceptance source: `1a1879ec6c6a86ed3de395e0e526dfbb84b93a80`, M1-S2 under roadmap revision
`2026-08-28.1`. Initial task core: `d738f43`. This component result does not close M1, choose a model,
change production routes, or represent a completed customer trial.

## Local results

Omen Node v22.22.0, root-owned disposable loopback PostgreSQL, independent per-test schemas:

```text
node --test gate7f/function-first/tasks/contracts.test.mjs \
  gate7f/function-first/tasks/postgres.test.mjs \
  gate7f/function-first/tasks/orchestrator.test.mjs
38 tests; 38 passed; 0 failed; 0 skipped
```

The PostgreSQL URL was supplied through `M1_TASK_PG_URL`; the service lifecycle belongs to root and is
not stopped by these tests. Generated fixtures only; no private/protected stores or models were opened.

Coverage includes exact capability/envelope versions and arguments, read-only/ask/safe-auto profiles,
current-session authority checks, grant revocation/expiry, stale project CAS, scoped request conflicts,
concurrent execution, publication rollback, lost acknowledgements, unknown test outcomes, cancellation
with bounded-drain evidence, exact owned restore, encrypted payloads and envelope-swap rejection,
raw-row canary exclusion, and fresh **process** LangGraph/PostgreSQL resume. The conversational tests
exercise planning, one repair after observed failure, finite-loop/deadline stops, invalid model commands,
approval/reload continuity, encrypted plans, explicit new-session grants, unknown-outcome blocks and
logout between steps. Planner input also excludes contents outside the current grant's selected paths,
including full revision references and out-of-scope historical proposals. Their deterministic executor
doubles are clearly marked; they are not MXC proof.

## Initial actual-runtime failures retained

The separate `project.integration.test.mjs` uses the real project adapter from
`769357425bb8613aac5bd3ac49d7ec70ec074719`, real PostgreSQL and disposable Windows files. Its child
process terminates with code 88 after staging a revision but before publication. A replacement service
observes the staged bytes and publishes exactly once, with the materialize method replaced by a throwing
guard to prove it was not called again. This scenario passed in both attempted integration runs.

The other two actual-runtime scenarios did **not** pass with a checkout used as MXC runtime root:
both returned `unavailable`, `sandbox-start-failed`, exit code 1, empty output, Node v22.22.0 and
`appcontainer-dacl`. The first attempt used the normal tool shell; the second used the authorized outer
shell. Both runs were **1/3 passed, 2/3 failed**. Therefore outer sandbox nesting is not an established
root cause. At this point actual governed MXC test execution was still unproved; the subsequent exact
Control proof below resolves that missing evidence without discarding these failed attempts.

The service retained unavailable status rather than presenting predicted output as execution. Existing
Gate 7E deployment evidence uses a compact staged runtime, copied Node and a dedicated transient source
directory with reviewed narrow ACLs, not a whole checkout/profile temporary folder. The next actual
runtime check must use that contract, preserving the fixed Gate 7E primitive and its target-only ACL
rules. No production ACL/runtime/service modification was made by this task.

## Subsequent real Control proof: 6/6 passed

Source `ad3e64ca1bbf210c4a8e65bc50f5f5efb4c353f8` plus project adapter
`769357425bb8613aac5bd3ac49d7ec70ec074719`; transferred source archive SHA-256
`ade244756dbfac185f7de1d01017e2ebfeafa80f22d9a78d60d730012a164de1`.
The read-only baseline verified `RUNA-CONTROL\Matthew` and clean Control repository
`f092d358a18f0ec0b6c2eaaeaf9a057b1d7f6d68`. The test did not update that checkout.

A fresh, isolated staging directory used copied Node v22.22.0, a compact QuickJS runtime and a dedicated
transient source directory. Only its four new owned directories received the reviewed target-only
metadata/read/traverse tuples. Existing ancestor and sampled descendant DACLs remained byte-stable.
No released MXC prepare/unprepare, broad ACL normalization, production store or persistent service was
used. The test's separate PostgreSQL instance listened only on its randomly allocated loopback port,
with a bounded watchdog and exact-data-directory cleanup.

Real preflight returned `runa2-sandbox-ready` through `appcontainer-dacl`. The actual transport suite
then passed **6/6**, no skipped tests, in **48.309 seconds**:

1. Inspect real files, observe failing sums, apply an exact-hash correction, observe passing sums, restore
   the owned prior revision, and observe the failure again. Corrected bytes remain in immutable history.
2. Kill the worker after materialization; the new process reconciles from disk and publishes once
   without invoking materialize again.
3. Cancel after native execution; preserve the actual receipt while the task remains cancelled.
4. Reject an escaping path and prove absent process/require/fetch/WebSocket interfaces inside real
   execution; retain deny-all networking, closed stdin and process-limit-one receipt settings.
5. Run an actual infinite loop and observe the unchanged 1.2-second QuickJS timeout.
6. Produce excessive actual output and observe output-limited status without partial successful output.

The test's PostgreSQL, watchdog, copied Node, compact runtime, transient files and staging directory were
removed. The source remains recoverable from Git and the pinned local archive. Postchecks verified
production runtime DACLs, production Node hash and the pre-existing PostgreSQL process IDs unchanged;
the Control repository still had the same head and clean tracked working tree.

The corrected **deployment envelope** is established: compact runtime + copied Node + owned traversable
transient directory works, unlike the previous checkout/profile-temp launch. Multiple envelope factors
and the host changed together, so this proof does not isolate one Omen setting as the sole root cause.
It does establish the real function path; it does not turn the initial failures into passes or claim an
Omen native-runtime repair. The retained aggregate evidence is `CONTROL-NATIVE-RESULTS-2026-08-28.json`.

The fixture's bounded in-flight cancellation semantics are explicit: cancel prevents further steps and
revision publication immediately; a started 1.2-second QuickJS/2-second process may finish and its actual
receipt must be retained. The UI must not say it killed that process. Authenticated surface/UI, actual
model planner routing, matched three-model acceptance and the customer's test remain root's integration
work; no benchmark-only substitute or production-ready claim is made here.
