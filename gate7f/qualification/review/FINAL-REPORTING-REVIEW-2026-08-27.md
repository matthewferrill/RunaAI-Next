# Independent post-inference reporting review

This records source and reporting reviews completed while the second matched endurance arm was
running. Final model/runtime disposition is owned by the qualification results report. No review
changed a frozen case, threshold, inference input, model answer or original finding.

## Judgment aggregation

The initial aggregation treated unresolved meaning as a definite failed threshold even when a pending
decision could meet that threshold. Commit `6ded88c` (root `11ca6b4`) preserves the current lower rate
and best-possible upper rate. A pending attempt can improve the upper bound only if no established
critical, ordinary, protocol or transport failure already prevents acceptance. Exact-contract and
critical failures remain definitive. Independent review passed 20 focused tests and a separate
55-combination fabricated sweep; no remaining actionable defect was identified.

## Source-bound final publication

The source-binding helper checks all 117 original identities, exact response content/native calls,
finish reasons and transport states against a byte-pinned anonymous packet. It does not infer semantic
meaning. The publication helper composes that binding with source-created Home transfer manifests,
the complete raw captures, the frozen package/corpus, exact completed functional prefixes, and the
explicit private candidate mapping. It recomputes the full capture summary; a caller's success boolean
cannot substitute for evidence.

Independent review of `a63a4c0` found one narrow clock gap: a separately repinned review-export manifest
could predate its captured functional prefix. Commit `dd4d5ad` (root `23a271c`) now requires export time
at or after both prefixes' final integration-summary event. Eighteen fabricated publication tests pass,
including a 1970 timestamp, between-arm timestamp, exact equality, and one-millisecond-early rejection.
No actual model outputs or model mapping were consulted to develop that correction.

## Mechanical adjudication and export

The independent reviewer examined every response before reading the first evaluator's judgments, then
resolved disagreements under the same rubric. Its exact 234 per-turn adjudications and original
records are retained byte-for-byte. Only nine semantic outcomes change: six unsupported-recurring-
meeting turns become ordinary errors, and three expanded source labels become semantically acceptable.
The latter keep their original frozen comparator failures and an explicit measurement limitation.
Both three-attempt critical clusters remain confirmed. No semantic review remains pending.

The root's final-adjudication helper verifies the immutable initial records before mechanically applying
those explicit decisions. It cannot replace a response, transport record, frozen deterministic finding
or denominator. Independent review reproduced the exact final bundle hashes, with three focused tests.
The separate final Home export helper binds seven exact evidence files, preserves failed arms and failed
restoration as evidence, has no import-time host/file effects, and uses a Home-gated create-only CLI.
Its three focused tests and independent source review passed. Export hashes alone do not prove runtime
success, semantic correctness or hardware attestation; full verification remains separate.

## Narrative review

The evaluator checked the draft report against the final ledgers after adjudication was retained and
the mapping was revealed. Counts, role failures, and the 46/54 source-label sensitivity calculation
matched. One wording correction was applied: the application exercise had three allowed proposals and
one refusal, not four emitted proposals. No qualification overstatement was identified in the reviewed
quality sections. Hardware and closeout placeholders were deliberately excluded from that review.

The full root repository suite passed 755 tests at this point; original v1, v2 and acceptance seals
also passed. This review is not a substitute for the final second-arm capture, power restoration,
Control comparison, or branch-push verification recorded at closeout.

## Completed matched arms and environment closeout

After both arms ended, the authority reviewer independently verified all seven files against the
Home-created final export pin and all 735 sealed package files. Gemma's stored summary reproduced
exactly: 256 requests including 131 in 3,600,015 ms; p50/p95/max client latency 1.689/23.100/24.295 s;
maximum actual prompt 12,230 tokens; 1,318 hardware samples with maximum gap 6.126 s; GPU maxima
78/75 C and 10,054/8,238 MiB; minimum free host memory 94.657 GiB. Exact UUIDs and 160-W limits held
throughout. The previously independently verified Qwen summary remained unchanged.

The root composed the final publication proof without regrading. The reviewer reran the retained
read-only reproduction driver and reproduced the compact result exactly. Full composition SHA-256:
`d361293996dc7c69111f4f424f5930d1e8d1fc2d5e0b602c46d1b50a3dde917a`.
No blocking provenance or measurement discrepancy was found. The final local regression run again
passed 755/755 tests, all three evaluation seals and both immutable initial judgment bundles.

Power restoration is supported both by the pinned operator's exact-UUID/260-W readback check and a
separate root live Home query after restoration. The reviewer checked the retained query result:
260/260 W, 47/40 C, zero loaded instances. Its file
`results/HOME-FINAL-STATUS-2026-08-27.json` has SHA-256
`f1502a9634b04edc64d9473711d27d6ba05aaabc5135b5b9ae647f7fdf3c6837`.
Control's separately retained final runtime/health responses deeply equal the during-run baseline,
with clean integration. Its `results/CONTROL-FINAL-STATUS-2026-08-27.json` has SHA-256
`44c0f24a5106b0fd60eaeb53597369915100788029145dab3991a73df1568de8`.
These final-status files are separate observations, not members of the seven-file Home export or
the full publication hash. They are not cryptographic host attestation or fresh human UI acceptance.
The reviewer made no host changes and did not revisit the frozen semantic judgments.
